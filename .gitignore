# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.venv/
venv/
ENV/

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Editors and OS
.DS_Store
._*
Thumbs.db
.ipynb_checkpoints

# Secrets and local API caches
.env
.envrc
.mp_cache/

# Training run trees: per-run outputs and provenance manifests, not part of the release
/outputs/
/runs/

# Generated per-experiment Markdown reports
/docs/results/

# Manuscript sources: drafted in this tree, not part of the code release.
# Their LaTeX build artifacts are covered by development/.gitignore conventions.
/docs/draft/
/docs/draft_new/

# Datasets. Raw data is external; manifests, splits and the processed MP archives ship.
/data/raw/**
!data/raw/mp/
!data/raw/mp/*.npz
/data/**/*.lmdb
/data/**/*.h5
/data/**/*.tar.gz

# Checkpoints and bulk arrays
*.pt
!src/equiparity/models/equiformer_v2/Jd.pt
*.ckpt
*.parquet

# Cached backbone features behind the frozen-backbone records
results/frozen_backbone/*_train.npz
results/frozen_backbone/*_val.npz
results/frozen_backbone/*_test.npz
results/frozen_backbone/*_ood.npz

# External model checkpoints and research codebases, obtained per docs/EXTERNAL_MODELS.md
third_party/

# Development scaffolding
/development/
