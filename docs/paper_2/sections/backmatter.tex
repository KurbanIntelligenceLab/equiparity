\subsection*{Data availability}

The QM9 dataset is publicly available (figshare record 978904, CC0); the 3,054 uncharacterized molecules (figshare record 3195404) are excluded as described in Methods. All crystal data derive from the Materials Project (CC-BY-4.0), retrieved on 2026-07-04 via its public API. Dataset manifests with content hashes, the split definitions, the identifiers of the 2,000-crystal centrosymmetric evaluation population in both coordinate variants, and the identifiers of the 1,000 augmentation crystals accompany the code, so that every dataset used here can be reconstructed exactly. The data generated in this study, comprising the per-structure violation vectors for all runs, the threshold curves, and the statistics behind every figure and table, are provided in machine-readable form in the same repository and are archived at \MISSING{persistent DOI of the archived data deposit (Zenodo or equivalent). Nature Communications requires a persistent identifier, not only a GitHub URL. Source Data for Figs.~2--5 and Supplementary Fig.~1 should be deposited in the same archive and referenced here}.

\subsection*{Code availability}

All code, comprising the matched-pair model builders and parity toggle, the verification gate, the training and evaluation pipelines, the strengthening experiments, the test suite verifying every group-theoretic claim, and the scripts that regenerate every figure and table, is available at \url{https://github.com/KurbanIntelligenceLab/equiparity} under the \VERIFY{state the licence (MIT was the intention). Confirm the repository is public at submission and carries the licence file, and that this statement matches the cover letter} and is archived at \MISSING{persistent DOI of the archived code release}.

\subsection*{Computing resources}

Training consumed 72.4 GPU-hours across the 84 grid runs on two consumer GPU classes; per-core compute, throughput, and memory are tabulated in Supplementary Table~9, with the caveat that the two GPU classes are not interchangeable and cross-core timing is not a like-for-like comparison. The full software environment and package pins are given in Supplementary Note~7.

\subsection*{Acknowledgements}

\MISSING{Acknowledgements and funding. Nature Communications requires all sources of funding to be declared, with grant numbers where applicable, for every author. State the funders and grant numbers, or state explicitly that the work received no specific funding. Acknowledge any computational resources or individuals who contributed but do not meet the authorship criteria, and do not thank anonymous referees or editors}

\subsection*{Author contributions}

C.P. conceived the study, designed and implemented the matched-pair parity-ablation and verification pipeline, conducted all computational experiments and benchmark runs, performed the data analysis, and wrote the original manuscript. E.S., M.K., and H.K. supervised the research, contributed to the interpretation of results, and reviewed and edited the manuscript. All authors read and approved the final manuscript.

\subsection*{Competing interests}

The authors declare no competing financial or non-financial interests.

\subsection*{Additional information}

\textbf{Supplementary information} is available for this paper. \textbf{Correspondence and requests for materials} should be addressed to Mustafa Kurban or Hasan Kurban.
